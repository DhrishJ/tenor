# yaml-language-server: $schema=./node_modules/envio/evm.schema.json
# GENERATED by scripts/gen-config.mjs from deployments/10143.json. Do not edit.
name: tenor-indexer
description: Tenor history and aggregates (current state is read from chain)
# Transaction hashes for explorer links and for joining BadDebtRealized to the
# Liquidated event that follows it in the same transaction.
field_selection:
  transaction_fields:
    - hash
contracts:
  - name: ScoreRegistry
    abi_file_path: ./abis/ScoreRegistry.json
    events:
      - event: AttestationSubmitted
      - event: LiquidationRecorded
  - name: TenorMarket
    abi_file_path: ./abis/TenorMarket.json
    events:
      - event: Deposit
      - event: Withdraw
      - event: CollateralDeposited
      - event: CollateralWithdrawn
      - event: Retiered
      - event: Borrowed
      - event: Repaid
      - event: Liquidated
      - event: BadDebtRealized
      - event: InterestAccrued
chains:
  - id: 10143
    start_block: 65456372
    # HyperSync is primary on Monad testnet; RPC takes over if it is unavailable.
    rpc:
      - url: https://testnet-rpc.monad.xyz
        for: fallback
    contracts:
      - name: ScoreRegistry
        address: "0x51c9e2F27d8E90A3A1feF894E6e60d1B739Ca098"
      - name: TenorMarket
        address: "0x66DCA766F95c3108b70ba4ee0a8f45E84E514408"
